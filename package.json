{
  "name": "site-python-mkdocs",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:css": "postcss frontend/styles.css -o docs/assets/styles.min.css",
    "build:js": "terser frontend/app.js -o docs/assets/app.min.js --compress --mangle",
    "build:site": "mkdocs build",
    "validate:html": "html5validator --root site --also-check-css --also-check-js"
    "minify:html": "html-minifier-terser --collapse-whitespace --remove-comments --minify-css true --minify-js true --input-dir site --output-dir site --file-ext html",
    "build": "npm run build:css && npm run build:js && npm run build:site && npm run validate:html && npm run minify:html"
  },
  "devDependencies": {
    "postcss": "^8.4.41",
    "postcss-cli": "^11.0.0",
    "autoprefixer": "^10.4.20",
    "cssnano": "^7.0.6",
    "terser": "^5.31.6",
    "html-minifier-terser": "^7.2.0"
  }
}
